<x-master-layout>
@php $isAr = app()->getLocale() === 'ar'; @endphp<div class="container-fluid sanad-page">
    <div class="sanad-header"><div><h1 class="sanad-title">{{ $isAr ? 'الإشعارات' : 'Notifications' }}</h1><div class="sanad-muted">{{ $isAr ? 'إشعارات الطلبات، المستندات، الفواتير، والتنبيهات العاجلة.' : 'Request, document, billing, and buzz notifications.' }}</div></div></div>
    <div class="row"><div class="col-lg-7"><div class="sanad-card"><div class="sanad-card-header">{{ $isAr ? 'إشعارات المنصة' : 'Platform Notifications' }}</div><div class="sanad-card-body">@forelse($notifications as $notification)<div class="border-bottom py-2"><strong>{{ formatNotificationTitle($notification) }}</strong><div class="sanad-muted">{{ formatNotificationMessage($notification) }}</div><small>{{ optional($notification->created_at)->format('Y-m-d H:i') }}</small></div>@empty<p class="sanad-muted">{{ $isAr ? 'لا توجد إشعارات.' : 'No notifications.' }}</p>@endforelse</div></div>{{ $notifications->links() }}</div><div class="col-lg-5"><div class="sanad-card"><div class="sanad-card-header">{{ $isAr ? 'تنبيهات البز العاجلة' : 'Buzz Notifications' }}</div><div class="sanad-card-body">@forelse($buzzAlerts as $buzz)<div class="border-bottom py-2"><span class="sanad-badge warn">{{ quick_status_label($buzz->priority) }}</span><div>{{ $buzz->message }}</div><small>{{ optional($buzz->created_at)->format('Y-m-d H:i') }}</small><div class="mt-2"><a class="sanad-btn secondary" href="{{ route('customer-portal.requests.show', ['id' => $buzz->booking_id, 'buzz_id' => $buzz->id]) }}#buzz-{{ $buzz->id }}">{{ $isAr ? 'فتح المحادثة' : 'Open Chat' }}</a></div></div>@empty<p class="sanad-muted">{{ $isAr ? 'لا توجد تنبيهات عاجلة.' : 'No buzz notifications.' }}</p>@endforelse</div></div></div></div>
</div>
</x-master-layout>
